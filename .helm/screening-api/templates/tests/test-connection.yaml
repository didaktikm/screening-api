apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "screening-api.fullname" . }}-test-connection"
  labels:
    {{- include "screening-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "screening-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
